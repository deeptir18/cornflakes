use super::{
    super::header_utils::{ProtoReprInfo},
    SerializationCompiler,
};
use color_eyre::eyre::Result;

pub fn compile(fd: &ProtoReprInfo, compiler: &mut SerializationCompiler) -> Result<()> {
    // unimplemented!()
    Ok(())
}
